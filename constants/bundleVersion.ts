// Single source of truth for the bundle version stamp.
// Import from HERE (not from useMLModel.native) so Metro never pulls
// react-native-fast-tflite into web / export bundles.
export const BUNDLE_VERSION = 'v17-autoformat-2026-03-06';
